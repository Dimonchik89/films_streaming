import React from "react";
import { Container, Layout, MainContent } from "../../components";

const page = () => {
  return (
    <Layout>
      <Container>
        <MainContent />
      </Container>
    </Layout>
  );
};

export default page;
